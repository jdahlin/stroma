# Improve pdf.js Rendering Performance (Reduce Flicker/Zoom Jank)

This doc moved.

- New location: [`docs/pdf/performance-plan.md`](./pdf/performance-plan.md)

Stroma already uses `pdfjs-dist` directly in the desktop renderer (not `react-pdf`).
