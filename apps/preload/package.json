{
  "name": "@repo/preload",
  "type": "module",
  "version": "0.1.6",
  "private": true,
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --format=cjs --sourcemap --tsconfig=tsconfig.json --outfile=dist/index.cjs --external:electron --conditions=import",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@repo/tsconfig": "workspace:*",
    "@types/node": "^22.12.0",
    "electron": "39.2.7",
    "esbuild": "^0.27.2",
    "typescript": "5.9.3"
  }
}
